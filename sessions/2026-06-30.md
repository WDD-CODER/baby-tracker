# Session — 2026-06-30

## Milestone 2 — Working folders + secrets hygiene

**Status:** Complete (pending human validation)

### Changes
- Created `plans/`, `bugs/`, `sessions/` with `.gitkeep` placeholders
- Created empty `.env` at repo root
- `.gitignore` already contains `.env*` (covers `.env`); no append needed

### Verification
- All three folders exist with `.gitkeep`
- `.env` exists and is ignored by git (not staged)
- No existing `.gitignore` rules modified

### Next
- Milestone 3: `README_WORKFLOW.md`

---

## Milestone 3 — Workflow README

**Status:** Complete (pending human validation)

### Changes
- Created `README_WORKFLOW.md` at repo root
- Documents three roles, the Brief→Plan→Carry→Execute→Validate→Verify→Test→Commit loop, folder layout, and Stage 0 file-based communication

### Verification
- `Test-Path README_WORKFLOW.md` → True
- No matches for forbidden patterns (AgentMemory, MCP, recall, handoff)
- Pure addition; no existing files modified

### Plan status
- `bootstrap_workflow_v1` — all three milestones implemented

---

## workflow_integration_v1 — Milestone 1 — Secrets hygiene for the real stack

**Status:** Complete (pending human validation)

### Changes
- Appended `firebase-applet-config.json` to `.gitignore`
- `.env*` already present (covers `.env`, `.env.local`, `.env.*.local`); no change needed
- `firebase-applet-config.json` was not tracked; `git rm --cached` not required

### Verification
- `git check-ignore firebase-applet-config.json` → echoes filename
- `git ls-files | Select-String firebase-applet-config.json` → empty
- No secret config staged

### Next
- Milestone 2: Create `_shared/current-state.md` + session import entry

---

## workflow_integration_v1 — Milestone 2 — Baseline state record

**Status:** Complete (pending human validation)

### Changes
- Created `_shared/current-state.md` (origin, stack, implemented features, backlog, PRD deltas)
- Imported AI Studio app recorded as baseline anchor for Context Capsule / memory backfill

### Verification
- `Test-Path _shared/current-state.md` → True
- File contains React 19, Firestore, and Backlog sections

---

## workflow_integration_v1 — Milestone 3 — Rule files (imported-baseline mode)

**Status:** Complete (pending human validation)

### Changes
- `.cursorrules`: added **Project baseline** section (extend, don't migrate; points to tech-stack + current-state)
- `CLAUDE.md`: added **Project baseline** section + current-state reference for Reviewer
- All existing Stage 0 rules preserved

### Verification
- Both files reference `_shared/tech-stack.md` and contain "baseline" instruction

---

## workflow_integration_v1 — Milestone 4 — Stage 0.5 human checklist

**Status:** Complete (pending human validation)

### Changes
- Created `SETUP_STATUS.md` with Done / Remaining sections and verbatim MCP config block

### Verification
- `Test-Path SETUP_STATUS.md` → True
- Contains `AGENTMEMORY_URL` and ~53 tools reference

---

## Import event

Imported AI Studio app as baseline; created `_shared/current-state.md`; reconciled secrets + rule files via `workflow_integration_v1`.

### Plan status
- `workflow_integration_v1` — all four milestones implemented (pending human/Reviewer validation)

---

# Claude Code Reviewer Session — 2026-06-30 (evening)

## What happened in this session

This was a **deep-exploration + planning session** run by Claude Code (Reviewer/DevOps role). No code was written. Deliverables are documents only.

### 1. Full codebase exploration

Claude Code read and analyzed every significant file in the repo:
- `src/types.ts`, `src/App.tsx` (~2000 lines, read in chunks), `server.ts`, `server/db.ts`
- `package.json`, `vite.config.ts`, `firebase-blueprint.json`, `firestore.rules`, `metadata.json`
- All `_shared/`, `plans/`, `sessions/`, `bugs/` files

### 2. Guidebook created — `_shared/agent-guide.md`

A 16-section comprehensive guidebook for all AI agents (Cursor, Claude Code, future agents) covering:
- Who does what in the Manual Bridge Workflow
- Full data model (Firestore paths, event types, all payload shapes)
- Complete API surface (every endpoint, request/response shape)
- UI architecture (tabs, bottom sheet pattern, all state variables)
- Coding conventions (Tailwind v4, Motion import, fetch patterns, icon rules)
- Hebrew string reference table
- What IS and IS NOT implemented (gap status)
- Known bugs
- Decision tree for adding new features
- Manual testing protocol

**This file is the primary onboarding document for any AI agent starting work on this project.**

### 3. Tech stack doc updated — `_shared/tech-stack.md`

Was a placeholder. Rewritten as the authoritative stack reference (all libraries, versions, what's NOT used, dev commands, env vars, hosting). Cursor and all agents must consult it before any tech decision.

### 4. PRD gap analysis — `plans/prd-gap-analysis.md`

User provided three PRD documents:
- `Baby_Tracker_PRD_v2.2` — the product contract
- `Baby_Tracker_DESIGN_FOUNDATION v1.0` — visual system, design tokens, wireframes
- `Workflow_Bootstrap_Guide` — the three-agent workflow guide (process, not product)

**Key finding — stack divergence (accepted, locked):**
PRD specifies Angular 19 + MongoDB. Codebase uses React 19 + Firestore. Too expensive to switch. All product feature intent from the PRD is valid; stack-specific implementation notes (Angular modules, Mongoose) are translated to React/Firestore equivalents.

**16 gaps identified**, grouped into 5 implementation waves:

| Priority | Gap | Description |
|---|---|---|
| P0 (Wave 1) | GAP-01 | SleepPayload missing `loggedByStart`/`loggedByEnd` — cross-parent attribution invisible |
| P0 (Wave 1) | GAP-02 | NutritionPayload missing `topUp` — breastfeeding top-up bottle (core medical feature) |
| P0 (Wave 1) | GAP-03 | UserSettings missing `babyProfile` (name, DOB, birth weight) |
| P1 (Wave 1) | BUG-01 | `getTodayNutritionSummary` uses UTC date, not Asia/Jerusalem — wrong-day bucketing |
| P1 (Wave 2) | GAP-06 | No undo toast after log |
| P1 (Wave 2) | GAP-07 | No Hebrew empty states on first-run timeline/dashboards |
| P1 (Wave 2) | GAP-08 | No poll-on-focus for concurrent sleep session (two parents, two phones) |
| P1 (Wave 2) | GAP-09 | Delete is destructive with no confirm — medical data at risk |
| P2 (Wave 2) | GAP-11 | Export has no date-range picker UI |
| P2 (Wave 2) | GAP-12 | No 12h sleep session "still sleeping?" warning |
| P2 (Wave 3) | GAP-10 | Heebo font not applied; design tokens not set in `src/index.css` |
| P1 (Wave 3) | GAP-04 | Semantic Icon System entirely missing (quality tiers BAD/OKAY/GOOD/GREAT) — the "trustable visible truth" pillar |
| P2 (Wave 3) | GAP-13 | Weight displayed in grams; PRD wants kg with 1 decimal |
| P1 (Wave 4) | GAP-05 | PWA not configured — app cannot be installed to home screen |
| P3 (Wave 5) | GAP-14 | Interactive dashboards (sleep 24h bands, diaper calendar, weight line) |
| P3 (Wave 5) | GAP-15 | Legend screen for Semantic Icon System |
| P3 (Wave 5) | GAP-16 | Export as its own top-level tab (currently buried in Settings) |

Full details with recommended implementation approach for each gap: see `plans/prd-gap-analysis.md`.

### 5. Plan Contract created — `plans/wave1_data_model_v1.md`

A fully declarative Plan Contract for Cursor to execute, covering Wave 1 (the 4 highest-priority gaps). 4 milestones:

| Milestone | Files | What it does |
|---|---|---|
| M1 | `src/types.ts` only | Add `loggedByStart`/`loggedByEnd` to SleepPayload; `topUp?: TopUpPayload` to NutritionPayload; `babyProfile?: BabyProfile` to UserSettings |
| M2 | `server.ts`, `server/db.ts` | Wire `activeParent` through `/api/sleep/toggle` so the new attribution fields get set on open and close |
| M3 | `src/App.tsx` | Baby profile UI in Settings tab + timezone bug fix in `getTodayNutritionSummary` |
| M4 | `src/App.tsx` | Sleep cards show both parent names; top-up bottle UI on breast feed edit sheet |

---

## Current project state (for next Context Capsule)

**Branch:** `main`  
**App:** Running React 19 SPA + Express + Firestore. Dev: `npm run dev`. Lint: `npm run lint`.  
**Stage:** Stage 0 (file-based handoffs — AgentMemory NOT yet wired)

**What's fully working:**
- 5 event types (NUTRITION / DIAPER / SLEEP / ACTIVITY / WEIGHT)
- Sleep state machine (toggle, shared session, live timer, location picker)
- Nutrition: bottle (offered/consumed ml) + breast (side, duration) + spit-up
- Diaper: full payload (contains, pee volume, poo amount/color/texture)
- Custom activities (create/delete, persisted to Firestore)
- Parent picker (localStorage), settings (parent names, default bottle type)
- Timeline with cards, edit/delete
- Hebrew RTL, Israel timezone (server-side)
- Excel export (ExcelJS, all 5 sheets)
- All stats API endpoints (/api/stats/nutrition, sleep, diapers, weight)
- Dashboards (CSS bars — functional but not yet interactive)

**What's next:**
- Cursor executes `plans/wave1_data_model_v1.md` — Milestone 1 first, then stop for review
- Claude Code reviews each milestone after Cursor finishes
- Human approves before each next milestone

**Known bug (not yet fixed):** `getTodayNutritionSummary` in `App.tsx` uses UTC date. Fixed in wave1 M3.

---

## Key file locations

| File | Purpose |
|---|---|
| `_shared/agent-guide.md` | Comprehensive AI agent guidebook (read this first) |
| `_shared/tech-stack.md` | Authoritative tech stack reference |
| `plans/prd-gap-analysis.md` | Full PRD gap analysis with all 16 gaps |
| `plans/wave1_data_model_v1.md` | **Active plan — ready for Cursor execution** |
| `src/types.ts` | All TypeScript types (source of truth for data model) |
| `src/App.tsx` | ~2000 lines — entire frontend in one component |
| `server.ts` | All Express routes |
| `server/db.ts` | All Firestore operations (ONLY place to touch Firestore) |

---

## wave1_data_model_v1 — Milestone 1 — Type layer

**Status:** Complete (pending human/Reviewer validation)

**Agent note:** Second Cursor agent. Checked git status and grep before starting — no in-progress code changes from another agent (`src/types.ts`, `server.ts`, `App.tsx` were untouched on `main`). Scoped to M1 only per plan.

### Changes — `src/types.ts` only

1. **SleepPayload** — added optional `loggedByStart?: ParentType` and `loggedByEnd?: ParentType` after `startLocation`
2. **NutritionPayload** — added optional `topUp?: TopUpPayload`; exported new `TopUpPayload` interface (`liquidType`, `consumedMl`)
3. **UserSettings** — added optional `babyProfile?: BabyProfile`; exported new `BabyProfile` interface (`name`, `dateOfBirth`, `birthWeightGrams`)

All fields optional — no migration needed.

### Verification

- `npm run lint` — **not run in this environment** (`npm` not on PATH; `node_modules` absent). Changes are type-only additions with no consumers yet; Reviewer should run `npm install && npm run lint` locally.
- No other files modified.

### Next

- Human approval → Milestone 2 (`server/db.ts`, `server.ts` — wire `activeParent` through `/api/sleep/toggle`)

---

## wave1_data_model_v1 — Milestone 2 — Backend sleep attribution

**Status:** Complete (pending human/Reviewer validation)

### Changes

- **`server/db.ts`** — comment on `saveEvent()` noting optional payload fields pass through passively
- **`server.ts`** — added `parseActiveParent()` helper; `/api/sleep/toggle` sets `sleep.loggedByStart` on open and `sleep.loggedByEnd` on close from `req.body.activeParent` (defaults to `PARENT_A`); accepts `location` alias for `startLocation`

### Verification

- IDE linter: no errors on modified files
- `npm run lint` — run locally (npm unavailable in agent shell)

---

## wave1_data_model_v1 — Milestone 3 — Baby profile Settings + timezone fix

**Status:** Complete (pending human/Reviewer validation)

### Changes — `src/App.tsx`

- **Sub-change A:** "פרטי תינוק" section in Settings with name, DOB, birth-weight stepper, and save button (`handleSaveBabyProfile` → `PUT /api/settings`)
- **Sub-change B:** `getTodayNutritionSummary` uses `Asia/Jerusalem` via `toLocaleDateString('sv-SE', { timeZone: 'Asia/Jerusalem' })` for both today and event date bucketing

### Verification

- IDE linter: no errors

---

## wave1_data_model_v1 — Milestone 4 — Sleep cards + top-up UI

**Status:** Complete (pending human/Reviewer validation)

### Changes — `src/App.tsx`

- **Sub-change A:** Sleep toggle sends `activeParent` in request body; timeline SLEEP cards show cross-parent attribution via `getSleepAttributionLabel()`
- **Sub-change B:** Collapsible "תוסף בקבוק" section in breast-feed edit sheet; saves/clears `nutrition.topUp`; timeline badge shows `+N מ״ל`
- **`src/types.ts`** — `topUp` type extended to `TopUpPayload | null` to support clearing on save

### Verification

- IDE linter: no errors on all modified files

### Manual test checklist (Reviewer)

1. `npm run lint` — exit 0
2. Sleep toggle: open as PARENT_A, close as PARENT_B → both `loggedByStart`/`loggedByEnd` in API response
3. Settings → save baby profile → reload → values persist
4. Breast feed edit → add top-up → save → badge on timeline card
5. Completed sleep with different start/end parents → card shows both names

### Plan status

- `wave1_data_model_v1` — all 4 milestones implemented (pending human/Reviewer validation)

---

## wave1_data_model_v1 — RE-APPLIED after GitHub sync (Firebase Client SDK migration)

**Status:** Complete — `npm run lint` passes (exit 0)

### Context

GitHub `main` received an AI Studio push `c7f58a3` ("feat: migrate to Firebase Client SDK") that significantly rewrote `src/App.tsx` (~3,600 lines), `server.ts`, `server/db.ts`, `src/types.ts`, and added PUMPING/VOMITING/swallowingNoises/quickRecorded features + a 6 AM "baby-day" model. It also deleted the workflow scaffolding files.

### Sync performed

1. `git stash push -u` — backed up all local wave1 work + planning docs
2. `git pull origin main` — fast-forwarded `bf5ea59` → `c7f58a3`
3. Restored planning docs (`plans/`, `_shared/agent-guide.md`, `sessions/2026-06-30.md`) from stash
4. Reset the 4 source files to upstream baseline (discarded stale wave1 edits made against the OLD codebase)
5. Re-applied all 4 milestones cleanly onto the new Firebase SDK codebase

### Re-applied changes (onto c7f58a3)

- **`src/types.ts`** — `BabyProfile`, `TopUpPayload` (as `TopUpPayload | null`), `UserSettings.babyProfile?`, `NutritionPayload.topUp?`, `SleepPayload.loggedByStart?`/`loggedByEnd?` (alongside existing PUMPING/VOMITING types)
- **`server.ts`** — `parseActiveParent()`; toggle sets `loggedByStart`/`loggedByEnd`; preserves `quickRecorded`; accepts `location` alias
- **`server/db.ts`** — passive-fields comment on `saveEvent()` (Firebase Client SDK `setDoc`)
- **`src/App.tsx`** — baby profile Settings section; `getTodayNutritionSummary` Asia/Jerusalem fix; `getParentDisplayName`/`getSleepAttributionLabel`; sleep toggle sends `activeParent`; timeline SLEEP attribution + top-up badge; top-up UI in breast edit sheet; pre-populate/save/reset top-up state

### Verification

- `npm install` — added firebase + other deps (node was present, deps were not installed)
- `npm run lint` (`tsc --noEmit`) — **exit 0, zero errors**

### Notes

- Backup stash `stash@{0}` retained until human confirms the merge; can be dropped after sign-off.
- Nothing committed/pushed yet — awaiting human approval before `git add` / commit / push to `main`.

